machine:
  timezone:
    America/Los_Angeles
  services:
    - docker
  node:
    version: 4.3.0
  environment:
    NODE_ENV: 'production'
deployment:
  production:
    branch: [master]
    commands:
      - if [ $CIRCLE_BRANCH == 'master' ]; then echo "You have access!" else echo "ACCESS DENIED!" fi
        
